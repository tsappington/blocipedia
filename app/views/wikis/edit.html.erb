<h1>Edit Wiki</h1>

<div class="row">
  <div class="col-md-4">
    <p>Guidelines for wikis</p>
    <ul>
      <li>Make it awesome.</li>
      <li>NOTE: <a href="https://github.com/adam-p/markdown-here/wiki/Markdown-Cheatsheet" target="_blank">Markdown</a> syntax supported!</li>
    </ul>
    <% if (current_user.premium? && @wiki.user_id == current_user.id) || current_user.admin? %>

      <p><strong>Wiki Collaborators:</strong></p>
      <ul>
        <% @collab_users.each do |collab_user| %>
          <li><%= collab_user.username %></li>
        <% end %>
      </ul>
      <%= form_tag(wiki_collab_remove_path(@wiki), remote: true, id: "RemoveCollab") do %>
        <input id="r_textbox" type="text" /><br />
        <%= select_tag :user_id, options_for_select(@collab_users.collect { |c| [ c.username, c.id ] }), id: 'remove_textbox' %>
        <%= submit_tag "Remove", method: :collab_remove, class: 'btn btn-success', data: {confirm: 'Are you sure you want to remove this collaborator?'} %>
      <% end %>

      <p><strong>Add New Collaborator:</strong></p>
      <%= form_tag(wiki_collab_add_path(@wiki), remote: true, id: "AddCollab") do %>
        <input id="a_textbox" type="text" /><br />
        <%= select_tag :user_id, options_for_select(@collaborators.collect { |c| [ c.username, c.id ] }), id: 'add_textbox' %>
        <%= submit_tag "Add", method: :collab_add, class: 'btn btn-success', data: {confirm: 'Are you sure you want to add this user?'} %>
      <% end %>

    <% end %>

  </div>
  <div class="col-md-8">

    <%= render partial: 'form', locals: { wiki: @wiki } %>

  </div>
</div>
